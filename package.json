{
  "name": "gp-attendance",
  "version": "1.0.0",
  "description": "YSA GP Attendance System",
  "scripts": {
    "gen-config": "node scripts/generate-firebase-config.js",
    "dedupe-classes": "node scripts/dedupe-classes.js",
    "test:rules": "node scripts/run-rules-test.js",
    "test:ui": "node scripts/run-ui-test.js"
  },
  "engines": {
    "node": ">=14"
  },
  "dependencies": {
    "firebase-admin": "^13.7.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^2.0.0",
    "firebase": "^9.22.1",
    "puppeteer-core": "^21.0.0"
  }
}
